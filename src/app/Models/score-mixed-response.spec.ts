import { ScoreMixedResponse } from './score-mixed-response';

describe('ScoreMixedResponse', () => {
  it('should create an instance', () => {
    expect(new ScoreMixedResponse()).toBeTruthy();
  });
});
