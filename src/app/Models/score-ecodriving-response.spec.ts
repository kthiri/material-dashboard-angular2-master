import { ScoreEcodrivingResponse } from './score-ecodriving-response';

describe('ScoreEcodrivingResponse', () => {
  it('should create an instance', () => {
    expect(new ScoreEcodrivingResponse()).toBeTruthy();
  });
});
