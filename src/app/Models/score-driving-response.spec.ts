import { ScoreDrivingResponse } from './score-driving-response';

describe('ScoreDrivingResponse', () => {
  it('should create an instance', () => {
    expect(new ScoreDrivingResponse()).toBeTruthy();
  });
});
